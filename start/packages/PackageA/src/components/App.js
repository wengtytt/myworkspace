import React from "react";
import ComponentA from "component-a";

export default function App(props) {
    return (
        <section>
            <h1>Sample container of the package A</h1>
            <ComponentA />
        </section>
    );
}
